% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_bootstrap_stability_kmeans.R
\name{bootstrap_stability_kmeans}
\alias{bootstrap_stability_kmeans}
\title{Bootstrap stability of k-means clusters (Jaccard index)}
\usage{
bootstrap_stability_kmeans(
  mat,
  k,
  B = 100,
  palette = "darkorange",
  base_size = 14,
  main = NULL
)
}
\arguments{
\item{mat}{A numeric matrix or data frame (samples in rows).}

\item{k}{Number of clusters (same as used in k-means).}

\item{B}{Number of bootstrap resamples (default: 100).}

\item{palette}{A color for the bars (default: "darkorange").}

\item{base_size}{Base font size for the plot (default: 14).}

\item{main}{Title of the plot.}
}
\value{
A list containing:
\describe{
  \item{jaccard}{Vector of Jaccard stability indices.}
  \item{plot}{A ggplot2 barplot of cluster stability.}
  \item{clusterboot_object}{The full clusterboot() output.}
}
}
\description{
This function computes the bootstrap stability of k-means clusters using
the Jaccard index from the \code{clusterboot()} function (package fpc).
It returns both the stability values and a ggplot2 barplot.
}
\examples{
\dontrun{
  mat_scaled <- scale(t(mat))
  res <- bootstrap_stability_kmeans(mat_scaled, k = 3)
  res$plot
}

}
