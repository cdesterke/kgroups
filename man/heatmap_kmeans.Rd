% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05_heatmap_kmeans.R
\name{heatmap_kmeans}
\alias{heatmap_kmeans}
\title{Heatmap annotated with k-means clusters}
\usage{
heatmap_kmeans(
  mat,
  clusters,
  palette = NULL,
  base_size = 12,
  main = "Heatmap + clusters"
)
}
\arguments{
\item{mat}{A numeric matrix or data frame (samples in rows).}

\item{clusters}{A vector of cluster assignments (same order as rownames(mat)).}

\item{palette}{A vector of colors or a palette name (default: NULL → automatic).}

\item{base_size}{Base font size for the heatmap (default: 12).}

\item{main}{Title of the heatmap.}
}
\value{
A pheatmap object.
}
\description{
This function generates a heatmap of a scaled matrix with samples annotated
by their k-means cluster assignment. The user can customize the color palette
and the base font size.
}
\examples{
\dontrun{
  mat_scaled <- scale(t(mat))
  res <- run_kmeans_with_pca(mat_scaled, k = 3)
  heatmap_kmeans(mat_scaled, res$clusters)
}

}
