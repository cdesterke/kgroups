% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_run_kmeans_with_pca.R
\name{run_kmeans_with_pca}
\alias{run_kmeans_with_pca}
\title{Run k-means clustering and visualize PCA with customizable palette and base size}
\usage{
run_kmeans_with_pca(
  mat,
  k,
  nstart = 50,
  seed = 123,
  palette = "Dark2",
  base_size = 14
)
}
\arguments{
\item{mat}{A numeric matrix or data frame (samples in rows).}

\item{k}{Number of clusters for k-means.}

\item{nstart}{Number of random initializations for k-means (default: 50).}

\item{seed}{Random seed for reproducibility (default: 123).}

\item{palette}{A color palette name (e.g. "Dark2", "Set1") or a vector of colors.}

\item{base_size}{Base font size for the PCA plot (default: 14).}
}
\value{
A list containing:
\describe{
  \item{clusters}{Cluster assignments (vector).}
  \item{kmeans_model}{The k-means model object.}
  \item{pca_plot}{A ggplot2 PCA plot colored by clusters.}
}
}
\description{
This function performs k-means clustering on a scaled matrix and returns
both the cluster assignments and a PCA visualization colored by clusters.
The user can customize the color palette and the base font size of the plot.
}
\examples{
\dontrun{
  mat_scaled <- scale(t(mat))
  res <- run_kmeans_with_pca(mat_scaled, k = 3, palette = "Set1", base_size = 16)
  res$pca_plot
  res$clusters
}

}
