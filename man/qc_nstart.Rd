% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_qc_nstart.R
\name{qc_nstart}
\alias{qc_nstart}
\title{Assess k-means stability across different nstart values}
\usage{
qc_nstart(mat, k = 2, nstart_values = c(1, 5, 10, 20, 50), replicates = 20)
}
\arguments{
\item{mat}{A numeric matrix or data frame (samples in rows).}

\item{k}{Number of clusters for k-means (default: 2).}

\item{nstart_values}{A numeric vector of nstart values to test.}

\item{replicates}{Number of repeated k-means runs per nstart value (default: 20).}
}
\value{
A ggplot2 boxplot object showing tot.withinss distribution per nstart.
}
\description{
This function evaluates the stability of k-means clustering by running
multiple initializations (`nstart`) and comparing the resulting
total within-cluster sum of squares (`tot.withinss`). It produces a
boxplot showing how sensitive the clustering is to initialization.
}
\examples{
\dontrun{
  mat_scaled <- scale(t(mat))
  qc_nstart(mat_scaled, k = 3)
}

}
